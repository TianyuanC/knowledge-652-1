import { Head } from "mdx-deck";
export { future as theme } from "mdx-deck/themes";

<Head>
    <title>Knowledge Session (1) 🤯</title>
</Head>

# Knowledge Session (1) 🤯

---

# Announcement

import { Appear } from "mdx-deck";

<Appear>
    <h2>Calendly</h2>
    <h2>Slack app</h2>
    <h2>MyReads</h2>
</Appear>

---

# React Dev Tools

---

import { Image } from "mdx-deck";

<Image src="https://raw.githubusercontent.com/TianyuanC/knowledge-652-1/master/img/react_dev.png" />

---

<Image src="https://raw.githubusercontent.com/TianyuanC/knowledge-652-1/master/img/react_org.png" />

---

# Questions?

---

# MyReads

---

import { SplitRight } from "mdx-deck/layouts";

export default SplitRight;

![](https://raw.githubusercontent.com/TianyuanC/knowledge-652-1/master/img/myread.png)

## [Repo](https://github.com/udacity/reactnd-project-myreads-starter)

## [Postman](https://www.getpostman.com/)

## [Books_API](https://reactnd-books-api.udacity.com)

---

# Questions?

---

# FAQ review

-   ES6
-   Editor: Sublime
-   Terminals
-   Git

---

import Code from "mdx-code";

# ES6

-   Map
-   Reduce
-   Filter

---

export default Code;

```javascript Array.prototype.Map
const counts = [1, 2, 3];
console.log(counts);
// [1, 2, 3]

const countsDouble = counts.map(count => count * 2);
console.log(countsDouble);
// [2, 4, 6]
```

---

export default Code;

```javascript Array.prototype.Reduce
const counts = [1, 2, 3];
console.log(`1 + 2 + 3 = ${1 + 2 + 3}`);
// 6

const sum = counts.reduce((acc, count) => acc + count);
console.log(sum);
// 6

const sumObj = counts.reduce((acc, count) => ({ output: acc.output + count }), {
    output: 0
});
console.log(sumObj);
// { output: 6 }
```

---

export default Code;

```javascript Softmax
/** e.g.
 * given softmax([1,2,3])
 * returns
 * e_sum = e^1 + e^2 + e^3
 * [e^1 / e_sum, e^2 / e_sum, e^3 / e_sum]
 */
function softmax(inputs) {
    const eSum = inputs.reduce((acc, input) => acc + Math.exp(input));
    return inputs.map(input => Math.exp(input) / eSum);
}

const inputs = [1, 2, 3];
const outputs = softmax(inputs);
console.log(outputs);
```

---

export default Code;

```javascript Array.prototype.Filter
const counts = [1, 2, 3];
console.log(counts);
// [1,2,3]

const odd = counts.filter(count => count % 2 == 1);
console.log(odd);
// [1,3]
```

---

# ES6

## Arrow Functions

---

export default Code;

```javascript Convert to Arrow Functions
const fn_0 = function() {};
fn_0();

const fn_1 = function(a) {
    console.log(a);
};
fn_1(1);

const fn_2 = function(a, b) {
    return a + b;
};
console.log(fn_2(1, 2));

const fn_3 = function(a) {
    return { a };
};
console.log(fn_3(1));

const fn_4 = function(a) {
    return function(b) {
        return Math.pow(a, b);
    };
};
console.log(fn_4(2)(3));
```

---

# ES6

## Promises

---

export default Code;

```javascript Promise chaining
setTimeout(function() {
    console.log("2s");
}, 2000);

const delay = t =>
    new Promise(resolve =>
        setTimeout(() => {
            console.log(`waited for ${(t * 1.0) / 1000}s`);
            resolve();
        }, t)
    );
// delay(1000)
//     .then(() => delay(1500))
//     .then(() => console.log(`done`));
```

---

export default Code;

```javascript Promise batching
const delay = t =>
    new Promise(resolve =>
        setTimeout(() => {
            console.log(`waited for ${(t * 1.0) / 1000}s`);
            resolve();
        }, t)
    );

Promise.all([delay(1000), delay(1500), delay(2000)]).then(() => {
    console.log("done");
});

// Promise.race([delay(1000), delay(1500), delay(2000)]).then(() => {
//     console.log("done");
// });
```

---

# There must be questions 🤣

---

# My Sublime

### Demo

---

# Thoughts?

---

# Terminal basics

### Demo

---

# All good?

---

# Git intro

### Demo

---

# Questions?

---

# Recap

-   React Dev Tools
-   MyRead
-   ES6
-   Editor: Sublime
-   Terminal
-   Git

---

# Until next time 👋
